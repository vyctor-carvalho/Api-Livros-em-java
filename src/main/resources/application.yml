spring:
  application:
    name: Api de livros
  datasource:
    url: jdbc:h2:mem:livros
    username: sa
    password: pass
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enable: true
      path: /dados-h2